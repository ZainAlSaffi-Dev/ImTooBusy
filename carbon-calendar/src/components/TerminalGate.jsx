// Deprecated — replaced by the new About section in v2 of the site.
// Left in place because the workspace doesn't allow file deletion.
// Nothing imports this; Vite will tree-shake it out of the bundle.
export default function TerminalGate() {
  return null;
}
